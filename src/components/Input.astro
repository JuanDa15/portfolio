---
const { id, name, ...others } = Astro.props;
---

<input
  type='text'
  id={id}
  name={name}
  class={`
      bg-gray-200 dark:bg-black
      placeholder-gray-500 dark:placeholder-gray-500
        border rounded-lg border-gray-400 dark:border-gray-700 
      text-slate-800 dark:text-white text-sm 
        block w-full p-2.5
        focus-visible:outline-electric-purple focus-visible:outline focus-visible:outline-2
        caret-electric-purple

    `}
  {...others}
/>
