---
import { getUrlWithBase } from '@/utils/url';

const { class: className } = Astro.props;
const url = getUrlWithBase('/img.jpg');
---

<img
  src={url}
  alt='juan_david_pic'
  class={`
    rounded-full 
    size-14 xl:size-16
    mb-4 ${className}
  `}
/>
